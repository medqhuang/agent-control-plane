# P6.5 Worklog

Updated: 2026-04-01
Stage: P6.5 Public Beta Release
Status: P6.5-1 Complete
Current Subgoal: P6.5-1 Release Surface Definition
Next Suggested Subgoal: P6.5-2 Desktop Delivery Baseline

This document freezes the first public Beta release surface based on current
implemented behavior. It is a scope-definition record, not a packaging or
installer record.

## P6.5-1 Goal

Define the smallest first public Beta that can be truthfully described and
trialed without writing unimplemented capabilities into the release promise.

This subgoal exists to lock:

- what is included
- what is excluded
- how users start it
- how each component is delivered
- which platforms are actually supported in the first Beta
- which prerequisites and limitations must be stated up front

## Release Unit

The first public Beta is defined as a technical source-run / source-install
Beta.

The release unit is the current repo, with these three user-visible runtime
surfaces:

- local `relay/`
- local `desktop/`
- remote `remote-agent/`

This is intentionally narrower than a polished product release. The first
public Beta does not promise an installer, a packaged daemon, or a hosted
service.

## Included Components

- local `relay`
- local `desktop`
- remote `remote-agent`
- `Kimi --wire`
- hosted session CLI:
  - `remote-agent kimi start --task "..."`
  - `remote-agent sessions`
  - `remote-agent watch <session_id>`
  - `remote-agent reply <session_id> --message "..."`
  - `remote-agent stop <session_id>`
- multi-remote baseline in the local desktop control plane

## Explicitly Excluded Components

- `Codex`
- `Claude Code`
- `attach`
- reconnect
- checkpoint persistence
- pending approvals replay
- control-plane event replay
- `remote-agent` restart restoration of hosted in-memory state
- provider execution-state recovery
- cloud relay / hosted relay
- desktop installer
- `remote-agent` PyPI package, wheel release, or OS package release

## Delivery Shape

### desktop

- delivered as repo source under `desktop/`
- current launch shape is `npm install && npm start`
- not an installer
- not a signed desktop distribution

### remote-agent

- delivered as repo source under `remote-agent/`
- installable as a Python package from the repo copy
- current deploy shape is Linux `systemd --user` with
  `scripts/install-systemd-user.sh`
- not a PyPI or system package release

### relay

- delivered as repo source under `relay/`
- started locally by the user as a FastAPI process
- required for the first public Beta
- not a cloud product and not a separately managed hosted service

## Minimal Trial Path

1. On the local machine, install relay dependencies and start `relay`.
2. On the same local machine, install desktop dependencies and start
   `desktop`.
3. Copy `remote-agent/` to a remote Linux host.
4. Install and start the remote user service.
5. Manually add `REMOTE_AGENT_RELAY_ENDPOINT`,
   `REMOTE_AGENT_CONTROL_BASE_URL`, and preferably
   `REMOTE_AGENT_REMOTE_NAME` to the remote env file.
6. Ensure `kimi --wire` is available on the remote host, or provide
   `KIMI_BIN`.
7. Run `remote-agent kimi start --task "..."` on the remote host.
8. Observe the session and any approval in the local desktop, then complete a
   local `approve / reject`.

## Support Matrix

| Surface | Platform | First Public Beta Status | Notes |
| --- | --- | --- | --- |
| local `desktop + relay` | Windows | supported | current first-Beta local path |
| local `desktop + relay` | macOS | not part of first-Beta promise | cross-platform cleanup is done, but this Beta does not bless macOS as shipped |
| local `desktop + relay` | Linux | excluded | not the current local target platform |
| remote `remote-agent` | Linux | supported | requires `python3`, `systemd --user`, `loginctl`, and linger |
| remote `remote-agent` | Windows / macOS | excluded | no deploy surface is defined |
| provider | `Kimi --wire` | supported | only formal provider in this Beta |
| provider | `Codex` / `Claude Code` | excluded | deferred to `P7` and `V2` |

## Minimum Prerequisites

- local Python environment able to run `relay`; use Python `3.10+` as the
  shared baseline
- local Node.js and npm able to run `desktop/`
- remote Linux with `python3`, `venv`, `systemctl --user`, and `loginctl`
- linger enabled for the remote user
- remote `kimi` CLI with `--wire`, or an explicit `KIMI_BIN`
- network reachability from remote hosts to local `relay`
- network reachability from local `relay` back to each remote-agent control
  address
- testers comfortable with manual env editing, SSH, and user-service setup

## Current Known Limitations

- `install-systemd-user.sh` does not yet provision
  `REMOTE_AGENT_RELAY_ENDPOINT`, `REMOTE_AGENT_CONTROL_BASE_URL`, or
  `REMOTE_AGENT_REMOTE_NAME`; first-Beta trial setup still needs manual env
  edits
- `desktop` is source-run only
- `relay` is a manually started local process
- `watch` is single-read, not continuous follow
- `attach` is not implemented
- `stop` rejects `approval_pending` and running-turn sessions
- `relay` and `remote-agent` are still in-memory; restart does not restore the
  prior control-plane state
- no provider execution-state `resume / reattach` promise exists

## Minimal Beta Deliverable Checklist

- root `README.md` with public Beta scope, quick-start path, support matrix,
  and known limits
- root `DEV.md` with internal release boundary and post-definition task order
- this `P6.5_worklog.md` as the frozen decision record for `P6.5-1`
- one minimal quick-start path that reaches a real local approval flow
- one first-wave screenshot or demo set
- issue templates for bug report and trial feedback
- first public Beta release notes

## Recommended Follow-up Order

1. `P6.5-2 Desktop Delivery Baseline`
   - freeze the local desktop delivery form
   - decide whether source-run remains the only release shape or whether a
     minimal archive distribution is needed
2. `P6.5-3 Remote-Agent Trial Install Surface`
   - tighten remote install docs and env-template wording
   - make the manual relay/control env requirements explicit
3. `P6.5-4 Quick Start And Trial Docs`
   - write one end-to-end path from startup to approval completion
4. `P6.5-5 Screenshots And Demo Capture`
   - capture the desktop control surface, multi-remote view, and approval flow
5. `P6.5-6 Issue Templates And Feedback Intake`
   - prepare trial feedback intake before public exposure
6. `P6.5-7 Release Notes And Launch Checklist`
   - finalize public messaging, known limitations, and the launch checklist

## Stable Baseline After P6.5-1

After this definition step:

- the first public Beta is fixed to `Kimi + local relay + local desktop +
  remote-agent + hosted session CLI + multi-remote baseline`
- `Codex`, `Claude`, installers, and `P8` recovery work remain explicitly
  outside the release promise
- the support promise is fixed to local Windows plus remote Linux
- later `P6.5` tasks should refine packaging and docs without reopening the
  release surface itself
